var a = prompt('Introdu A'),
    b =  prompt('Introdu B'),
    c =  prompt('Introdu C');
 
var max = Math.max(a,b,c),
    min = Math.min(a,b,c);
alert(`Numarul Maxim este: ${max} si numarul Minim este: ${min}`);
